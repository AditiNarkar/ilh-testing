function calculateSum(data) {
  let sum = 0;
  for (let num of data) {
    sum += num;  // Using for...of loop for summing elements
  }
  console.log("eeeee")
  return sum;
}
function calcOne() {
  return "one";
}
